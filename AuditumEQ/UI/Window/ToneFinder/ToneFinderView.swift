import SwiftUI

struct ToneFinderView: View {
    var body: some View {
        PlaceholderView(
            title: "Tone Finder",
            summary: "Large frequency sweep control + sine tone generator at a safe output level. Lets the user pinpoint the pitch of their tinnitus and copy it into the notch filter.",
            sessionRef: "Session 15",
            symbol: "tuningfork"
        )
    }
}
