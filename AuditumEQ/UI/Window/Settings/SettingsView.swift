import SwiftUI

struct SettingsView: View {
    var body: some View {
        PlaceholderView(
            title: "Settings",
            summary: "Launch at login, global Reference Mode shortcut, device auto-switching, AutoEQ library, profile backup location, acknowledgments.",
            sessionRef: "Session 17",
            symbol: "gearshape"
        )
    }
}
