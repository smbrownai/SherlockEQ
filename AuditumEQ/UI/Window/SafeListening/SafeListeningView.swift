import SwiftUI

struct SafeListeningView: View {
    var body: some View {
        PlaceholderView(
            title: "Safe Listening",
            summary: "Live level estimate, session dose bar, 7-day dose history, ceiling and notification preferences. NIOSH 3 dB exchange-rate explainer.",
            sessionRef: "Session 12",
            symbol: "shield.lefthalf.filled"
        )
    }
}
